---
name: scenario-dev
description: 'Behavior-first (BDD) development workflow. Turns a feature, change request, or bug fix into a frozen set of Given/When/Then acceptance scenarios, then implements and *proves* each one with an automated test before calling the work done. Use whenever the user wants scenario-driven, behavior-driven, or acceptance-test-driven development; wants to nail down expected behavior as concrete scenarios first; mentions BDD, Given/When/Then, acceptance criteria, or "define the behavior before coding"; or wants every behavior backed by a passing test rather than just "tasks checked off". Progress here is measured in scenarios verified.'
---

Follow the instructions in ./workflow.md.
